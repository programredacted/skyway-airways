Set the working GOAL for this session. Treat everything after this command as the
target end state.

Rules:
1. Restate the goal as a numbered list of concrete, testable acceptance criteria.
   Every criterion must be verifiable by running code or a test — no vague ones.
2. Write/refresh GOAL.md in the repo root with these criteria as unchecked boxes:
   - [ ] criterion
3. Identify which criteria are already met by the current codebase (check them off
   with evidence: the test name or command that proves it).
4. Do NOT start implementing. Wait for /loop.

Goal: $ARGUMENTS
